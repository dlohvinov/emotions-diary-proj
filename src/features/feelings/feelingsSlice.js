import { createSlice } from '@reduxjs/toolkit';

export const feelingsSlice = createSlice({
  name: 'feelings',
  initialState: {

  },
  reducers: {

  },
});

export const {} = feelingsSlice.actions;

export default feelingsSlice.reducer;
